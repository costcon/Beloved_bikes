<div class="container">
   <div class="row">
    <div class='col-4 offset-1'>
     <h1 class="heading08"><span>Requests</span>リクエスト一覧</h1>
    </div>
   </div>
   <%= render partial: 'layouts/flash' %>
    <div class="row hoge">
      <div calss="col-10 mx-auto">
      <% if @reservations.empty? %>
        <h4>リクエストはまだありません</h4>
      <% else %>
       <table border="1" class="table">
         <thead class="table-primary">
           <tr>
             <th>バイク名</th>
             <th>利用開始日時</th>
             <th>返却日時</th>
             <th>ユーザー名</th>
             <th></th>
           </tr>
         </thead>
         <tbody>
           <% @reservations.each do |reservation| %>
           <tr>
             <!--<td>-->
             <!-- <#%= attachment_image_tag(@bike, :bike_image, :fill, 30, 30, fallback: "no_image_bike.jpg") %>-->
             <!-- <#%= @bike.name %>-->
             <!--</td>-->
             <td><%= reservation.start_time %></td>
             <td><%= reservation.end_time %></td>
             <!--<td><#%= @user.nickname %></td>-->
             <td>
              <%= link_to '確認する', reservation_path(reservation), class: "btn btn-sm btn-success" %>
              <%= link_to 'チャット', root_path, class: "btn btn-sm btn-info" %>
             </td>
           </tr>
           <% end %>
         </tbody>
       </table>
      <% end %>
      </div>
    </div>
 </div>