<div class="container col-xs-12 col-sm-12 col-md-10">
  <div class="text-center">
    <div class="mt-5">
      <strong>本当に退会しますか？</strong>
      <div>
        <p>退会する場合は、『退会する』をクリックしてください。</p>
      </div>
    </div>

  　　<%= link_to "退会しない", user_path(@user), class: "btn btn-sm btn-primary edit_customer_#{@user.id}" %>
      <%= link_to "退会する", user_withdraw_path(current_user), method: :put, "data-confirm" => "本当に退会しますか？", class: "btn btn-sm btn-danger" %>
  </div>
</div>
